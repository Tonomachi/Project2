@model IEnumerable<Project2.Models.Nganhhoc1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Monhoc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Monhoc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Nganh }) |
            @Html.ActionLink("Details", "Details", new { id=item.Nganh }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Nganh })
        </td>
    </tr>
}

</table>
